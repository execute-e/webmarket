import fallbackKolonka from './kolonka.png';
import fallbackComputer from './computer.png';
import fallbackLaptop from './laptop.png';
import fallbackPhone from './phone.png';
import fallbackTablet from './tablet.png';
import fallbackTv from './tv.png';

export const images = {
    kolonka: {
        fallback: fallbackKolonka
    },
    computer: {
        fallback: fallbackComputer
    },
    laptop: {
        fallback: fallbackLaptop
    },
    phone: {
        fallback: fallbackPhone
    },
    tablet: {
        fallback: fallbackTablet
    },
    tv: {
        fallback: fallbackTv
    }
}